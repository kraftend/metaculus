# =============================================================================
# Staging Environment Deployment
# =============================================================================
#
# This workflow deploys to a persistent staging environment on every push to main.
# Unlike PR previews, staging:
# - Uses a fixed app name (metaculus-staging)
# - Runs all processes (app, worker, cron)
# - Uses NeonDB main branch (not ephemeral branch)
# - Creates/reuses a persistent Redis database via Fly.io CLI (managed Upstash)
# - Never auto-stops machines
#
# Required Secrets:
# -----------------
# - FLY_API_TOKEN: API token from Fly.io (run `fly tokens create deploy`)
# - FLY_STAGING_SECRETS: Multi-line secret containing all app secrets in KEY=value format
#     Example:
#       SECRET_KEY=django-secret-key-here
#       OPENAI_API_KEY=sk-xxx
#       SENTRY_DSN=https://xxx@sentry.io/xxx
# - NEON_PROJECT_ID: Your Neon project ID
# - NEON_API_KEY: API key from Neon Console
#
# Optional Variables:
# -------------------
# - FLY_ORG: Fly.io organization (defaults to 'personal')
# - STAGING_APP_NAME: Fly app name (defaults to 'metaculus-staging')
# - FLY_REDIS_REGION: Redis region for Fly.io managed Upstash (defaults to 'iad')
# =============================================================================

name: Staging Deployment

on:
  push:
    branches:
      - main

concurrency:
  group: staging-deploy
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  deployments: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  STAGING_APP_NAME: ${{ vars.STAGING_APP_NAME || 'metaculus-staging' }}

jobs:
  # ===========================================================================
  # Setup Job - Get commit info
  # ===========================================================================
  setup:
    name: Setup
    runs-on: ubuntu-latest
    outputs:
      short_sha: ${{ steps.short_sha.outputs.sha }}
      image_tag: ${{ steps.image_tag.outputs.tag }}
    steps:
      - name: Get short SHA
        id: short_sha
        run: echo "sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Generate image tag
        id: image_tag
        run: echo "tag=main-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

  # ===========================================================================
  # Build Docker Image
  # ===========================================================================
  build-image:
    name: Build Docker Image
    needs: setup
    runs-on: ubuntu-24.04
    outputs:
      image_url: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Mutable tag - always points to latest staging build
            type=raw,value=latest
            # Immutable tag - unique per commit (for rollbacks)
            type=raw,value=${{ needs.setup.outputs.image_tag }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          target: all_runners
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

  # ===========================================================================
  # Deploy to Fly.io Staging
  # ===========================================================================
  deploy-staging:
    name: Deploy Staging
    needs: [setup, build-image]
    runs-on: ubuntu-latest
    environment:
      name: Staging
      url: ${{ steps.deploy.outputs.url }}
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Fly.io CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Get NeonDB connection string
        id: neon
        uses: neondatabase/create-branch-action@v5
        with:
          project_id: ${{ secrets.NEON_PROJECT_ID }}
          branch_name: main
          api_key: ${{ secrets.NEON_API_KEY }}
          username: neondb_owner

      - name: Get or Create Fly Redis Database
        id: redis
        run: |
          REDIS_NAME="metaculus-staging"
          FLY_ORG="${{ vars.FLY_ORG || 'personal' }}"
          REDIS_REGION="${{ vars.FLY_REDIS_REGION || 'iad' }}"
          
          # Check if database already exists
          if flyctl redis list --org "${FLY_ORG}" | grep -q "${REDIS_NAME}"; then
            echo "✅ Found existing Redis database: ${REDIS_NAME}"
          else
            echo "Creating new Redis database: ${REDIS_NAME}"
            flyctl redis create \
              --name "${REDIS_NAME}" \
              --region "${REDIS_REGION}" \
              --no-replicas \
              --enable-eviction \
              --org "${FLY_ORG}"
            echo "✅ Created new Redis database: ${REDIS_NAME}"
          fi
          
          # Get Redis connection URL from status (parse text output)
          REDIS_URL=$(flyctl redis status "${REDIS_NAME}" | grep -i "Private URL" | awk -F'= ' '{print $2}' | tr -d ' ')
          
          if [ -z "$REDIS_URL" ]; then
            echo "❌ Failed to get Redis URL from status"
            flyctl redis status "${REDIS_NAME}"
            exit 1
          fi
          
          # Set outputs
          echo "redis_cache_url=${REDIS_URL}" >> $GITHUB_OUTPUT
          echo "redis_mq_url=${REDIS_URL}" >> $GITHUB_OUTPUT
          echo "Redis URL retrieved successfully"

      - name: Deploy to staging
        id: deploy
        run: |
          # Define app URLs
          APP_DOMAIN="${{ env.STAGING_APP_NAME }}.fly.dev"
          PUBLIC_APP_URL="https://${APP_DOMAIN}"

          # Check if app exists, create if not
          if ! flyctl apps list | grep -q "${{ env.STAGING_APP_NAME }}"; then
            echo "Creating new Fly app: ${{ env.STAGING_APP_NAME }}"
            flyctl apps create ${{ env.STAGING_APP_NAME }} \
              --org ${{ vars.FLY_ORG || 'personal' }}
          fi

          # Import static secrets from FLY_STAGING_SECRETS
          echo "${{ secrets.FLY_STAGING_SECRETS }}" | flyctl secrets import \
            --app ${{ env.STAGING_APP_NAME }} \
            --stage
          
          # Set dynamic secrets (URLs, database, redis)
          flyctl secrets set \
            APP_DOMAIN="${APP_DOMAIN}" \
            PUBLIC_APP_URL="${PUBLIC_APP_URL}" \
            DATABASE_URL="${{ steps.neon.outputs.db_url_with_pooler }}" \
            REDIS_CACHE_URL="${{ steps.redis.outputs.redis_cache_url }}" \
            REDIS_MQ_URL="${{ steps.redis.outputs.redis_mq_url }}" \
            --app ${{ env.STAGING_APP_NAME }} \
            --stage

          # Deploy using the pre-built image
          # VM memory/cpu settings are defined per-process in fly.toml
          flyctl deploy \
            --app ${{ env.STAGING_APP_NAME }} \
            --image ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.setup.outputs.image_tag }} \
            --ha=false \
            --now

          # Scale all processes for staging (app, worker, cron all running)
          echo "Scaling processes: app=1, worker=1, cron=1"
          flyctl scale count app=1 worker=1 cron=1 \
            --app ${{ env.STAGING_APP_NAME }} \
            --yes

          # Output the app URL
          echo "url=${PUBLIC_APP_URL}" >> $GITHUB_OUTPUT
          echo "✅ Deployed staging to: ${PUBLIC_APP_URL}"

      - name: Post deployment summary
        run: |
          echo "## 🚀 Staging Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Resource | Details |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🌐 **URL** | ${{ steps.deploy.outputs.url }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 **Image** | \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.setup.outputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 🗄️ **PostgreSQL** | NeonDB main branch |" >> $GITHUB_STEP_SUMMARY
          echo "| ⚡ **Redis** | Fly Redis \`metaculus-staging\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔖 **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Processes" >> $GITHUB_STEP_SUMMARY
          echo "| Process | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🌐 **app** | ✅ Running |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 **worker** | ✅ Running |" >> $GITHUB_STEP_SUMMARY
          echo "| ⏰ **cron** | ✅ Running |" >> $GITHUB_STEP_SUMMARY

